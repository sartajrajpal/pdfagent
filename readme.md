Add this file to this Github repo once you fork it: 'https://github.com/VikParuchuri/marker/'. Add a .env file with your OpenAI API and PubMed keys. Then the agent should work! The code and data for the other portion of the project (the data pipeline) cannot be made public, as the data the pipeline uses is healthcare data and is therefore subject to many constraints. 
